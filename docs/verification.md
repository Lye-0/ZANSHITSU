# 検証記録 — 2026-09-18 写真とアイテム操作の改修

## 自動テスト

- `pnpm test`: 35件成功。
- `mise run build`: TypeScript・Viteの公開用ビルド成功。
- 全214写真の参照・実在・未参照ファイルの排除を確認。
- 水槽の16到達状態、保存値検証、旧保存の移行、全20段階の進行、重複取得防止を確認。

## 実ブラウザ

検証専用オリジン `http://127.0.0.1:53947/` を使用。4187のユーザーの保存をテストデータで置換していない。

Playwright MCPで初期状態から4部屋・20仕掛けを実操作し、最後の扉まで到達した。途中の進行状態や正解値の直接代入はしていない。画像点検には別途、対象の状態を用意した専用フィクスチャも使用した。

- 13収納: 解錠時に取得されない → 取っ手を押す → 中の物を押す → 空の写真になる。
- 10設置先: 接写へ入るだけでは使用されない。未選択で設置先を押しても使用されない。持ち物の選択と設置先のクリックで使用される。
- レンチは使った後も持ち歩き、その他の設置部品は持ち物欄から移る。
- 4枚の銘板を順に設置して最終錠を操作し、ハンドルで終了する。
- 20仕掛け・17観察対象の実画面を撮影し、文字、穴の数、投影の高さ、輪郭、部品の座標を目視確認。
- 時計の短針・長針を直接選んで文字盤へ移動し、解錠できる。
- 開いた収納の未取得アイテムが再読み込み後にも残る。
- 接写への移動、持ち物の表裏確認からの復帰、天井への移動で選択を解除する。
- コンソールの警告・エラーなし。

agent-browserスキルでも390×844のブラウザを操作した。天井から元の壁へ復帰し、引き出しの入力・開閉・取得を実操作した。Playwright MCPでは同じ幅で巻き鍵の選択解除・表裏確認・設置・時計の操作・再読み込みを確認した。小引き出しと右方向ボタンの重なりを修正し、物の中心を押して接写へ入れることを確認した。横スクロールなし。

## 目視で行った調整

- 古い紙と時計の数字を写真に含め、動く針を金属の写真へ変更。
- 配管は実物の継手、水面は状態写真を採用。
- 誤ったフィルムの穴数、譜面の線数、設置済みレンズが消えた収納写真を再制作。
- 解錠後の数字、板、フィルムが消えないように状態ごとの表示位置を調整。
- 透過板の収納を開けた状態は、その状態の写真に写ったガラスを使用して二重表示を防止。
- 扉に実際のハンドルが写る写真を採用。
- 持ち物、表裏の刻印、最終扉の銘板も写真へ統一。

検証画像はリポジトリ外の検証用ディレクトリに保存。候補・アトラスはリポジトリに残さない。

## 未検証

実機iOS Safari / Android Chrome、初見プレイヤーの所要時間と難易度。ブラウザのスマホ幅での確認は実機検証ではない。

## 操作性の修正（2026-09-18）

- 時計は短針・長針の軸寄り、中央、先端でクリックできることを確認。390px幅で長針の中央からドラッグし、値が3から6へ変わることを確認。重なった針は同じ位置の再タップで選び分け、キーボードでも選択・解錠できる。
- 工具は接写写真上の両端と持ち手中央の3か所から取得できる。以前の検証のように、成功する一点に限定しない。
- 鏡の上下を一つの領域へ統合。どちらをクリックしても4つのダイヤルを持つ同じ接写が開く。
- 鏡の文字は正確な2715の写真部品を左右反転して配置。実画面を一時的に反転して2715と読めることを目視確認。2715で解錠、収納を開き、栓を取得できる。入力欄も新しい写真の窪みへ再配置。
- ヒントを1→2→3→2→1と移動でき、開示済み段階は3のまま保持。見出しを再クリックすると閉じる。
- 水槽は写真外に現在量と目標量を表示。7回の注水で現在量が 3/5/0 → 3/2/3 → 6/2/0 → 6/0/2 → 1/5/2 → 1/4/3 → 4/4/0 と更新され、収納を開いた後も目標4/4/0 Lが残る。390px幅で写真との重なり・横スクロールなし。
- agent-browserでスマホ幅の設定・手掛かりへの導線を実操作し、Playwright MCPで各修正を座標クリック・ドラッグ・状態遷移で確認。
- テスト35件と公開用ビルド成功。コンソール警告・エラーなし。

鏡の統合後は全43対象。採用写真は214点（使わなくなった鏡の接写を削除し、数字の写真部品を追加）。今回の対象外の仕掛けは上記の前回通し検証を参照。

## 設置済み部品の刻印と初回の完成図閲覧（2026-09-18）

- 排水口の浮き輪部品・透過板の台ともに、設置後の写真を「設置済み」から押すと裏面の刻印を表示する。下の三角で直前の仕掛けへ戻り、環の向きも維持する。部品を持ち物へ戻したり、再設置したりしない。
- インストール済みの保存状態から再開しても刻印確認が利用できる。刻印を読んで両仕掛けを解錠し、収納を開いた後も再確認できることを実操作で確認。
- 初回の完成図閲覧だけで盤面が変わる現象は検証用の新規状態では再現しなかった。一方、持ち物を選択すると盤面全体が操作不能となり、紙を見ることで選択が解除されて操作できる条件を確認した。この選択による機構の無効化を削除し、機構を操作すると持ち物選択を解除する。
- 盤面を最初に開いた時点で初期配列を保存する `beginPuzzle` を追加。初回閲覧、移動後の閲覧、再読み込みの各段階で同じ配列が維持されることを確認。
- 初期配列 724/5空6/831 のまま完成図を初めて往復し、3を動かして724/536/8空1にした後も往復・再読み込みで保持。完成図を先に見た場合も同じ初期配列で開始し、全20手を操作してレンズ取得まで確認。
- 紙未閲覧かつレンチ選択中でも3の板を動かせ、レンチは消費せず選択だけが解除される。
- 空きに隣接する板だけを操作可能にし、「空きの隣の板を動かす」と「完成図を見る」を写真外へ表示。完成図から直接同じ盤面へ戻れる。
- 390px幅で設置済み部品の欄と刻印画面、盤面と完成図を目視確認。持ち物欄の不要な横スクロールなし。コンソール警告・エラーなし。
- テスト37件・公開用ビルド成功。Wi-Fiのユーザー保存は変更せず、53947の検証用オリジンを使用した。
